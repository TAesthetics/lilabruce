# Site Configuration

To hand over the site configuration:

```bash
node scripts/write-atomic.mjs /tmp/site.json src/lib/og/site.json
```

This writes the site metadata including title, type, card, and color settings.
